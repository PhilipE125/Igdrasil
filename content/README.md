# Content

This directory stores source content that is loaded by the app at runtime.

## Contents

- `legal/`: markdown for the `/privacy` and `/terms` routes.

Keep runtime-owned copy here rather than in the repository root so content and code stay clearly separated.